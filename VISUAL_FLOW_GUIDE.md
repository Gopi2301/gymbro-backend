# GymBro Application Flow - Visual Guide

## 🎯 Three User Types, One Ecosystem

```
┌─────────────────────────────────────────────────────────────────┐
│                        GYMBRO ECOSYSTEM                          │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  SUPER ADMIN     │
│  (Content)       │
└────────┬─────────┘
         │
         │ Creates & Manages
         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     EXERCISE LIBRARY                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • Bench Press (Chest, Compound, Barbell)                 │  │
│  │ • Squat (Quadriceps, Compound, Barbell)                  │  │
│  │ • Deadlift (Back, Compound, Barbell)                     │  │
│  │ • ... (all exercises available to coaches)               │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Uses
                             ▼
                    ┌──────────────────┐
                    │     COACH        │
                    │  (Programming)   │
                    └────────┬─────────┘
                             │
                             │ Creates
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ROUTINE: "Push/Pull/Legs"                     │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  SPLIT 1: Push Day (Monday)                             │   │
│  │  ┌────────────────────────────────────────────────────┐ │   │
│  │  │ 1. Bench Press      - 4 sets x 8-12 reps, 90s rest │ │   │
│  │  │ 2. Overhead Press   - 3 sets x 8-12 reps, 90s rest │ │   │
│  │  │ 3. Tricep Dips      - 3 sets x 12-15 reps, 60s rest│ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  SPLIT 2: Pull Day (Wednesday)                          │   │
│  │  ┌────────────────────────────────────────────────────┐ │   │
│  │  │ 1. Deadlift         - 4 sets x 5-8 reps, 120s rest │ │   │
│  │  │ 2. Pull-ups         - 3 sets x AMRAP, 90s rest     │ │   │
│  │  │ 3. Barbell Rows     - 3 sets x 8-12 reps, 90s rest │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  SPLIT 3: Leg Day (Friday)                              │   │
│  │  ┌────────────────────────────────────────────────────┐ │   │
│  │  │ 1. Squat            - 4 sets x 8-12 reps, 120s rest│ │   │
│  │  │ 2. Leg Press        - 3 sets x 12-15 reps, 90s rest│ │   │
│  │  │ 3. Leg Curls        - 3 sets x 12-15 reps, 60s rest│ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Picks/Assigned
                             ▼
                    ┌──────────────────┐
                    │      USER        │
                    │   (Training)     │
                    └────────┬─────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
    ┌───────────────────┐     ┌───────────────────┐
    │  OPTION A:        │     │  OPTION B:        │
    │  Follow Routine   │     │  Track Standalone │
    └─────────┬─────────┘     └─────────┬─────────┘
              │                         │
              ▼                         ▼
┌─────────────────────────┐   ┌─────────────────────────┐
│  Monday: Push Day       │   │  Today's Workout        │
│  ┌───────────────────┐  │   │  ┌───────────────────┐  │
│  │ Bench Press       │  │   │  │ Bench Press       │  │
│  │ Set 1: 100kg x 8  │  │   │  │ Set 1: 100kg x 8  │  │
│  │ Set 2: 100kg x 10 │  │   │  │ Set 2: 100kg x 10 │  │
│  │ Set 3: 100kg x 12 │  │   │  │ Set 3: 100kg x 12 │  │
│  │ Set 4: 100kg x 8  │  │   │  │ Set 4: 100kg x 8  │  │
│  └───────────────────┘  │   │  └───────────────────┘  │
│  ✅ Completed           │   │  ✅ Logged              │
└─────────────────────────┘   └─────────────────────────┘
```

---

## 📊 Database Flow Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                         DATABASE TABLES                           │
└──────────────────────────────────────────────────────────────────┘

SUPER ADMIN FLOW:
┌─────────────┐
│super_admin  │
│ - id (uuid) │
│ - email     │
│ - name      │
└──────┬──────┘
       │ creates
       ▼
┌─────────────────┐
│   exercises     │
│ - id (uuid)     │
│ - createdBy ────┘ (references super_admin.id)
│ - name          │
│ - primaryMuscle │
│ - type          │
│ - equipment     │
└────────┬────────┘
         │
         │ used by
         │
COACH FLOW:
┌─────────────┐
│   coach     │
│ - id (uuid) │
│ - name      │
│ - gymName   │
└──────┬──────┘
       │ creates
       ▼
┌─────────────────┐
│   routines      │
│ - id (uuid)     │
│ - coachId ──────┘ (references coach.id)
│ - name          │
│ - description   │
│ - isPublic      │
└────────┬────────┘
         │ has many
         ▼
┌──────────────────┐
│ routine_splits   │
│ - id (uuid)      │
│ - routineId ─────┘ (references routines.id)
│ - name           │
│ - dayOrder       │
└────────┬─────────┘
         │ contains
         ▼
┌───────────────────┐
│routine_exercises  │
│ - id (uuid)       │
│ - splitId ────────┘ (references routine_splits.id)
│ - exerciseId ─────┐ (references exercises.id)
│ - sets            │
│ - reps            │
│ - restTimeSeconds │
└───────────────────┘
         │
         │
USER FLOW:
┌─────────────┐
│   users     │
│ - id (uuid) │
│ - email     │
│ - name      │
└──────┬──────┘
       │
       ├─────────────────┐
       │                 │
       │ picks           │ logs
       ▼                 ▼
┌──────────────┐   ┌──────────────────┐
│user_routines │   │  workout_logs    │
│ - id (uuid)  │   │  - id (uuid)     │
│ - userId ────┘   │  - userId ───────┘
│ - routineId ─┐   │  - exerciseId ───┐ (standalone)
└──────────────┘   │  - routineExerciseId ─┐ (from routine)
                   └────────┬─────────┘
                            │ contains
                            ▼
                   ┌──────────────────┐
                   │  workout_sets    │
                   │  - id (uuid)     │
                   │  - workoutLogId ─┘
                   │  - setNumber     │
                   │  - reps          │
                   │  - weight        │
                   │  - rpe           │
                   └──────────────────┘
```

---

## 🔄 User Journey Examples

### Journey 1: Beginner Following a Coach's Routine

```
1. User signs up
   └─ Record created in `users` table

2. Coach assigns "Beginner Full Body" routine to user
   └─ Record created in `user_routines`
   └─ Links: user.id + routine.id

3. Monday: User opens app, sees "Full Body Day A"
   └─ App queries:
      - user_routines → routines → routine_splits
      - Finds split for Monday
      - Gets all routine_exercises for that split

4. User starts workout, logs Bench Press
   └─ Creates workout_log
   └─ Links: user.id + routineExercise.id

5. User logs each set:
   └─ Set 1: Creates workout_set (workoutLog.id, set 1, 50kg, 10 reps, RPE 6)
   └─ Set 2: Creates workout_set (workoutLog.id, set 2, 50kg, 10 reps, RPE 7)
   └─ Set 3: Creates workout_set (workoutLog.id, set 3, 50kg, 10 reps, RPE 8)

6. User completes all exercises in the split
   └─ Multiple workout_logs created (one per exercise)
   └─ Multiple workout_sets per log

7. Next week: User can see progress
   └─ Query workout_logs filtered by:
      - userId
      - exerciseId (to see specific exercise history)
      - Date range
```

### Journey 2: Advanced User Tracking Custom Workout

```
1. User decides to do their own workout (not following a routine)

2. User selects "Bench Press" from exercise library
   └─ Queries `exercises` table

3. User creates workout log
   └─ Creates workout_log
   └─ Links: user.id + exercise.id
   └─ routineExerciseId = NULL (standalone workout)

4. User logs sets:
   └─ Set 1: 100kg x 8, RPE 7
   └─ Set 2: 100kg x 10, RPE 8
   └─ Set 3: 100kg x 12, RPE 9
   └─ Set 4: 100kg x 8, RPE 10

5. User adds notes: "Felt strong today, increase weight next time"
   └─ Saved in workout_log.notes

6. Next session: User can see last workout
   └─ Query workout_logs WHERE userId = X AND exerciseId = Y
   └─ Order by completedAt DESC
   └─ Shows: "Last time: 4 sets, avg 9.5 reps @ 100kg"
```

---

## ✅ Schema Validation Summary

| Requirement                        | Table(s)                                     | Status |
| ---------------------------------- | -------------------------------------------- | ------ |
| Super admin creates exercises      | `super_admin`, `exercises`                   | ✅     |
| Track exercise creator             | `exercises.createdBy`                        | ✅     |
| Coach creates routines             | `coach`, `routines`                          | ✅     |
| Routines have multiple days/splits | `routine_splits`                             | ✅     |
| Splits contain exercises           | `routine_exercises`                          | ✅     |
| Exercise programming details       | `routine_exercises` (sets, reps, rest, etc.) | ✅     |
| Users pick routines                | `user_routines`                              | ✅     |
| Users track routine workouts       | `workout_logs.routineExerciseId`             | ✅     |
| Users track standalone exercises   | `workout_logs.exerciseId`                    | ✅     |
| Detailed set tracking              | `workout_sets`                               | ✅     |
| Progress over time                 | Query `workout_logs` + `workout_sets`        | ✅     |
| Data integrity (cascades)          | All FK relationships                         | ✅     |
| UUID for all IDs                   | All tables                                   | ✅     |

**VERDICT: 100% COMPLETE** ✨

Your schema perfectly supports all required flows!
